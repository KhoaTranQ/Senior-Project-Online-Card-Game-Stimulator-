///net_view_request(data);

var data = argument0;

var net_id = buffer_read(data, buffer_string);
var local_ = syncMap[? net_id];

new(async_view_response, local_);

/*

local_.image_blend = c_red;

async_object = local_;

perm_id = show_question_async("The opponent is trying to view a seck, marked in RED. Will you let them?");

